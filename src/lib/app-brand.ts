/** User-visible product name (window title, UI labels, default NDI names). */
export const APP_DISPLAY_NAME = "SabbathCue"

export const DEFAULT_NDI_SOURCE_NAME = `${APP_DISPLAY_NAME} Output`
export const DEFAULT_NDI_ALT_SOURCE_NAME = `${APP_DISPLAY_NAME} Alt`
