---
title: Speech-to-Text Engines
description: Choose between local Vosk and cloud Deepgram, and configure either with the right environment variables.
icon: Mic
---

import { Callout } from "fumadocs-ui/components/callout";
import { Card, Cards } from "fumadocs-ui/components/card";

SabbathCue supports two STT engines, and you can swap between them at runtime
from the settings dialog. They map cleanly to two different operating
modes: zero-marginal-cost local inference, or offload-to-the-cloud
streaming.

<Cards>
  <Card
    title="Vosk (local, free, verse-focused)"
    description="Runs the Vosk speech recognition toolkit on your machine — no API key, no internet, no per-minute billing. Uses a constrained verse-reference grammar for fast Bible reference detection."
  />
  <Card
    title="Deepgram (cloud, paid)"
    description="Streams audio to Deepgram's WebSocket endpoint with a REST fallback. Lower latency and higher accuracy on long-form sermons; needs a paid API key."
  />
</Cards>

## Option 1: Vosk (local, default)

Vosk is an offline speech recognition toolkit that transcribes audio in
real time using a verse-focused constrained grammar. It recognizes Bible
book names, chapter and verse numbers, and navigation keywords. For
full-sermon transcript coverage with general vocabulary, use Deepgram.

To install the Vosk model:

1. Download a model from [alphacephei.com/vosk/models](https://alphacephei.com/vosk/models)
2. Place it in `models/vosk/` (the app expects `vosk-model-small-en-us` by default)
3. Or set `SABBATHCUE_VOSK_MODEL_DIR` to point to your model folder

The model is required before Vosk transcription starts. The app shows
model installation status in Settings → Speech Recognition.

## Option 2: Deepgram

Deepgram is a cloud speech-to-text service that streams audio over a
WebSocket. SabbathCue also has a REST fallback that re-uploads short windows if
the WebSocket disconnects.

### 1. Get an API key

Sign up at [deepgram.com](https://deepgram.com/) and copy your API key.

### 2. Add it to `.env`

Create a `.env` file in the project root:

```bash
DEEPGRAM_API_KEY=dg_your_key_here
```

Restart `bun run tauri dev` so the new key is picked up.

### 3. Switch to Deepgram in the app

Open Settings → STT and choose **Deepgram**. The status indicator shows
WebSocket health in real time; if it falls back to REST, you'll see that
labeled too.

![Settings dialog with the STT engine picker showing Vosk and Deepgram options](/docs/screenshots/settings-stt-picker.png)

*Settings → STT lets you flip between local Vosk and cloud Deepgram at runtime. The connection status indicator under the picker turns green when Deepgram's WebSocket is healthy and amber when it falls back to REST. **Click to expand.***

## Switching engines at runtime

Both engines are wired into the same transcript stream, so you can switch
without restarting the session. The active translation, sermon context,
and queue state are preserved across the swap.

## Comparing the two

| | Vosk (local) | Deepgram (cloud) |
| --- | --- | --- |
| API cost | Free | Per-minute billing |
| Internet required | No, after the model is installed | Yes (WebSocket) |
| Recognition mode | Verse-focused grammar | Keyword-boosted streaming |
| Transcript coverage | Bible references + navigation | Full general speech |
| Punctuation | None (plain text) | Excellent |
| Works offline | Yes | No |

Vosk uses a constrained grammar tuned for Bible verse references, so it
excels at picking out book names and numbers during a sermon. For full
transcript fidelity of the entire message, Deepgram is the recommended
option. Pick Vosk for the no-cost offline verse-detection path,
Deepgram for the best full-sermon transcription quality on a reliable
network.
